class Numerico {
  edad = 25;
  salario = 3500.75;
  altura = 1.78;
  telefono = 3124567890;
  cantidad = 5;

  sumar(a, b) { return a + b; }
  promedio(a, b) { return (a + b) / 2; }
  multiplicar(a, b) { return a * b; }
  convertirMetros(km) { return km * 1000; }
  restar(a, b) { return a - b; }
}
